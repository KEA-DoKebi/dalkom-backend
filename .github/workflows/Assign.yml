name: Auto Assign Reviewers

on:
  pull_request

jobs:
  assign-reviewers:
    runs-on: ubuntu-latest
    steps:
    - name: Assign reviewers
      uses: peter-evans/create-pull-request@v5.0.2
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        team-reviewers: Developer/back
