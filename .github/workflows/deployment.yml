name: Continuous Deployment

on:
  pull_request:
    branches:
      - release
    types: [closed]

jobs:
  deploy_to_Kakao:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true && github.base_ref == 'dev'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag dokebi-was:latest

    - name: Tag the Docker image
      run: docker tag dokebi-was:latest dokebi-shop.kr-gov-central-1.kcr.dev/dokebi-repo/dokebi-was:latest

    - name: Login to Docker Registry
      run: docker login dokebi-shop.kr-gov-central-1.kcr.dev --username ${{ secrets.DOCKER_REGISTRY_USERNAME }} --password ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

    - name: Push the Docker image
      run: docker push dokebi-shop.kr-gov-central-1.kcr.dev/dokebi-repo/dokebi-was:latest
